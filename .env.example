# Civitai API 토큰 (선택). 없으면 public API로 호출한다.
# 실제 토큰을 public repo에 커밋하지 말 것.
CIVITAI_TOKEN=

# Slack Incoming Webhook URL. send_slack.py --send 시 필요(선택).
# 비밀번호처럼 취급한다. 실제 값을 public repo / README / 코드 / 커밋 로그에 남기지 말 것.
SLACK_WEBHOOK_URL=
